# sakz-bot — Cleanup & Hardening Pass (2026-06-17)

Behavior-preserving cleanup of dead/dormant code, verified in loops with
`py_compile`, a 26-module import smoke test, and the existing test suite
(155 checks across 5 test files). All checks passed after every change.

## 1. Unreachable code removed
- `sakz_bot.py` — deleted two legacy fall-through blocks after `return`
  statements in the diagnostic-reason handler (one was explicitly commented
  "now unreachable"). ~71 lines.
- `sakz_memory.py` — removed a duplicate dead `return conn` (second copy could
  never execute).

## 2. Unused imports removed (24 names; re-exports & availability guards kept)
- `sakz_bot.py`: `ta`, `dataclass`, `field`, `Optional`, `FancyArrowPatch`,
  `Arc`, `Circle`, and two local `import matplotlib.dates as mdates`.
- `sakz_ccxt.py`: `sys` (local). `sakz_conviction.py`: `timedelta`, `sys`.
- `sakz_db.py`: `os`. `sakz_memory.py`: `asyncio`. `sakz_paper.py`: `Dict`.
- `sakz_prime.py`: `datetime`, `timezone`. `sakz_risk.py`: `field`.
- `sakz_scanner.py`: `timedelta`, `field`, `Optional`.
- `sakz_signal_logic.py`: `timedelta`. `sakz_ws.py`: `datetime`, `timedelta`.
- `xgboost_train.py`: `json`, `roc_auc_score`.
- Intentionally kept: `from sakz_db import (...)` blocks marked
  `# noqa: F401 re-exported` and consumed by `sakz_backtest_hist.py`, plus
  ML imports inside `try/except` availability guards (`_XGB_AVAILABLE`,
  `_RF_AVAILABLE`, `_HAVE_SCHEMA`).

## 3. Dead local assignments removed (15; all side-effect-free)
- `sakz_scanner.py`: `t2_raw`/`t3_raw` (LONG and SHORT) — Step-1 raw targets
  that were superseded by the proportional `_t2_dist`/`_t3_dist` method;
  `established_cross` (computed, never used).
- `sakz_bot.py`: `is_correlated` (redundant — logic uses `is_corr_long` /
  `is_corr_short` directly); leftover help-text blocks in `manual_command`
  (the command was refactored to point users to `/pro`); unused color/style
  constants (`AMBER_LT`, `AMBER_XDK`, `HEADER_BG`, two `SOFT`); `cache_note`,
  `best_band`.

## Verification
- `python3 -m py_compile *.py` → all 31 files OK.
- Import smoke test → 26/26 modules import cleanly.
- Test suite → test_phase0, test_prime (51), test_prime_integration,
  test_autoscan_lifecycle (17), test_signal_behavior (56) → all pass.
- Net: 28,651 → 28,565 source lines.

## Notes / observations (not changed — need your call)
- The XGBoost/RF predictor imports in `sakz_bot.py` are bound but never
  invoked there; live ML scoring runs via `sakz_scanner.py`. Confirm whether
  `sakz_bot` was meant to use them directly.
- `except ...: pass` handlers (~43) are typed (no bare `except:`), so they do
  not swallow `KeyboardInterrupt`/`SystemExit`; left as intentional best-effort.
- Could not exercise live exchange/Telegram/network paths offline; verification
  was static analysis + import smoke + the existing pure-logic/DB test suite.
